<div class="flex justify-between items-center mb-8">
    <h1 class="font-serif text-5xl font-black"><?= htmlspecialchars($pageTitle) ?></h1>
    <a href="/admin/blog/create" class="bg-primary text-void border-2 border-primary px-6 py-2 hover:bg-transparent hover:text-primary transition font-mono text-xs uppercase tracking-widest font-black">+ Neuer Artikel</a>
</div>

<div class="border-2 border-ink overflow-hidden">
    <table class="w-full">
        <thead class="bg-ink text-paper border-b-2 border-ink">
            <tr>
                <th class="px-6 py-4 text-left font-mono text-xs uppercase tracking-widest">Titel</th>
                <th class="px-6 py-4 text-left font-mono text-xs uppercase tracking-widest">Status</th>
                <th class="px-6 py-4 text-left font-mono text-xs uppercase tracking-widest">Datum</th>
                <th class="px-6 py-4 text-right font-mono text-xs uppercase tracking-widest">Aktionen</th>
            </tr>
        </thead>
        <tbody>
            <?php if (!empty($articles)): ?>
                <?php foreach ($articles as $item): ?>
                    <tr class="border-b-2 border-ink hover:bg-paper">
                        <td class="px-6 py-4"><?= htmlspecialchars(substr($item['title'], 0, 50)) ?></td>
                        <td class="px-6 py-4">
                            <span class="px-3 py-1 border-2 text-sm font-mono <?= $item['published'] ? 'border-primary text-primary' : 'border-ink text-ink' ?>">
                                <?= $item['published'] ? '✅ Veröffentlicht' : '❌ Entwurf' ?>
                            </span>
                        </td>
                        <td class="px-6 py-4 text-sm text-muted"><?= $item['createdAt']->format('d.m.Y H:i') ?? date('d.m.Y') ?></td>
                        <td class="px-6 py-4 text-right">
                            <a href="/admin/blog/<?= htmlspecialchars($item['id']) ?>/edit" class="text-primary hover:text-ink transition text-sm mr-4 font-mono font-black">Bearbeiten</a>
                            <button onclick="deleteBlog('<?= htmlspecialchars($item['id']) ?>')" class="text-primary hover:text-ink transition text-sm font-mono font-black">Löschen</button>
                        </td>
                    </tr>
                <?php endforeach; ?>
            <?php else: ?>
                <tr>
                    <td colspan="4" class="px-6 py-8 text-center text-muted">Keine Artikel vorhanden</td>
                </tr>
            <?php endif; ?>
        </tbody>
    </table>
</div>
