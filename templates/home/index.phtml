<!-- Hero Section -->
<section class="bg-gradient-to-r from-primary to-secondary text-white py-20">
    <div class="container mx-auto px-4 text-center">
        <h1 class="text-5xl font-bold mb-4">Professionelle Webpräsenz zum Nulltarif</h1>
        <p class="text-xl mb-8 text-gray-100">CMS, Blog, Shop und Newsletter – alles in einer modernen Website</p>
        <div class="flex gap-4 justify-center">
            <a href="/shop" class="bg-white text-primary px-8 py-3 rounded font-semibold hover:bg-gray-100 transition">Zum Shop</a>
            <a href="/blog" class="border-2 border-white px-8 py-3 rounded font-semibold hover:bg-white hover:text-primary transition">Blog lesen</a>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="py-16 bg-gray-100">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold text-center mb-12">Was bieten wir?</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="bg-white rounded shadow p-8 text-center">
                <div class="text-4xl mb-4">📰</div>
                <h3 class="text-2xl font-bold mb-2">CMS & Blog</h3>
                <p class="text-gray-600">Verwalte deine Inhalte einfach mit unserem CMS. News, Blog-Artikel und mehr.</p>
            </div>
            <div class="bg-white rounded shadow p-8 text-center">
                <div class="text-4xl mb-4">🛒</div>
                <h3 class="text-2xl font-bold mb-2">Online Shop</h3>
                <p class="text-gray-600">Verkaufe deine Produkte online mit integriertem Checkout und Zahlungsabwicklung.</p>
            </div>
            <div class="bg-white rounded shadow p-8 text-center">
                <div class="text-4xl mb-4">📧</div>
                <h3 class="text-2xl font-bold mb-2">Newsletter</h3>
                <p class="text-gray-600">Halte deine Kunden informiert mit unserem Newsletter-System.</p>
            </div>
        </div>
    </div>
</section>

<!-- Latest News -->
<?php if (!empty($latestNews)): ?>
<section class="py-16">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold mb-12">Neueste News</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <?php foreach ($latestNews as $item): ?>
                <article class="bg-white rounded shadow hover:shadow-lg transition overflow-hidden">
                    <?php if (!empty($item['imageUrl'])): ?>
                        <img src="<?= htmlspecialchars($item['imageUrl']) ?>" alt="News" class="w-full h-40 object-cover">
                    <?php else: ?>
                        <div class="w-full h-40 bg-gradient-to-r from-primary to-secondary"></div>
                    <?php endif; ?>
                    <div class="p-6">
                        <time class="text-gray-500 text-sm">
                            <?= $item['createdAt']->format('d.m.Y') ?? date('d.m.Y') ?>
                        </time>
                        <h3 class="text-xl font-bold mt-2 mb-2 line-clamp-2"><?= htmlspecialchars($item['title']) ?></h3>
                        <p class="text-gray-600 line-clamp-2"><?= htmlspecialchars(substr(strip_tags($item['content']), 0, 80)) ?>...</p>
                        <a href="/news/<?= htmlspecialchars($item['id']) ?>" class="text-primary hover:text-blue-600 transition font-semibold mt-4 inline-block">Lesen →</a>
                    </div>
                </article>
            <?php endforeach; ?>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- Latest Blog Posts -->
<?php if (!empty($latestBlog)): ?>
<section class="py-16 bg-gray-50">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold mb-12">Blog-Highlights</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <?php foreach ($latestBlog as $item): ?>
                <article class="bg-white rounded shadow hover:shadow-lg transition overflow-hidden">
                    <?php if (!empty($item['imageUrl'])): ?>
                        <img src="<?= htmlspecialchars($item['imageUrl']) ?>" alt="Blog" class="w-full h-40 object-cover">
                    <?php else: ?>
                        <div class="w-full h-40 bg-gradient-to-r from-secondary to-accent"></div>
                    <?php endif; ?>
                    <div class="p-6">
                        <time class="text-gray-500 text-sm">
                            <?= $item['createdAt']->format('d.m.Y') ?? date('d.m.Y') ?>
                        </time>
                        <h3 class="text-xl font-bold mt-2 mb-2 line-clamp-2"><?= htmlspecialchars($item['title']) ?></h3>
                        <p class="text-gray-600 line-clamp-2"><?= htmlspecialchars(substr(strip_tags($item['content']), 0, 80)) ?>...</p>
                        <a href="/blog/<?= htmlspecialchars($item['id']) ?>" class="text-primary hover:text-blue-600 transition font-semibold mt-4 inline-block">Lesen →</a>
                    </div>
                </article>
            <?php endforeach; ?>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- Featured Products -->
<?php if (!empty($featuredProducts)): ?>
<section class="py-16">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold mb-12">Empfehlenswerte Produkte</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <?php foreach ($featuredProducts as $product): ?>
                <article class="bg-white rounded shadow hover:shadow-lg transition overflow-hidden">
                    <?php if (!empty($product['imageUrl'])): ?>
                        <img src="<?= htmlspecialchars($product['imageUrl']) ?>" alt="Produkt" class="w-full h-40 object-cover">
                    <?php else: ?>
                        <div class="w-full h-40 bg-gray-200 flex items-center justify-center">
                            <span class="text-gray-400">Kein Bild</span>
                        </div>
                    <?php endif; ?>
                    <div class="p-6">
                        <h3 class="text-xl font-bold mb-2 line-clamp-2"><?= htmlspecialchars($product['name']) ?></h3>
                        <p class="text-gray-600 line-clamp-2 mb-4"><?= htmlspecialchars($product['description'] ?? '') ?></p>
                        <div class="flex justify-between items-center">
                            <span class="text-2xl font-bold text-primary"><?= number_format($product['price'], 2, ',', '.') ?> €</span>
                            <a href="/shop/<?= htmlspecialchars($product['id']) ?>" class="bg-primary text-white px-4 py-2 rounded hover:bg-blue-600 transition">Details</a>
                        </div>
                    </div>
                </article>
            <?php endforeach; ?>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- Newsletter Section -->
<section class="py-16 bg-primary text-white">
    <div class="container mx-auto px-4 max-w-2xl text-center">
        <h2 class="text-4xl font-bold mb-4">Newsletter abonnieren</h2>
        <p class="text-lg mb-8">Erhalte die neuesten Updates direkt in dein Postfach</p>
        <form id="newsletter-form" class="flex gap-2">
            <input type="email" id="newsletter-email" required placeholder="deine@email.de" class="flex-1 px-4 py-3 rounded text-gray-900">
            <button type="submit" class="bg-white text-primary px-6 py-3 rounded font-semibold hover:bg-gray-100 transition">
                Abonnieren
            </button>
        </form>
    </div>
</section>
