<footer class="bg-void text-paper py-24 relative overflow-hidden">
    <div class="absolute inset-0 text-9xl font-black opacity-5 flex items-center justify-center">
        <?= date('Y') ?>
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-12">
            <!-- Brand -->
            <div class="border-r-2 border-primary pr-8">
                <h3 class="font-serif text-2xl font-black text-paper mb-4">ZeroWeb</h3>
                <p class="text-muted">Professionelle Webpräsenz zum Nulltarif.</p>
            </div>

            <!-- Links -->
            <div class="border-r-2 border-primary pr-8">
                <h4 class="font-mono text-xs uppercase tracking-widest text-primary mb-4">Navigation</h4>
                <ul class="space-y-2 text-muted">
                    <li><a href="/news" class="hover:text-paper transition">News</a></li>
                    <li><a href="/blog" class="hover:text-paper transition">Blog</a></li>
                    <li><a href="/shop" class="hover:text-paper transition">Shop</a></li>
                </ul>
            </div>

            <!-- Support -->
            <div class="border-r-2 border-primary pr-8">
                <h4 class="font-mono text-xs uppercase tracking-widest text-primary mb-4">Support</h4>
                <ul class="space-y-2 text-muted">
                    <li><a href="#" class="hover:text-paper transition">Kontakt</a></li>
                    <li><a href="#" class="hover:text-paper transition">Datenschutz</a></li>
                    <li><a href="#" class="hover:text-paper transition">Impressum</a></li>
                </ul>
            </div>

            <!-- Newsletter -->
            <div>
                <h4 class="font-mono text-xs uppercase tracking-widest text-primary mb-4">Newsletter</h4>
                <form id="footer-newsletter-form" class="flex flex-col gap-4">
                    <input type="email" id="footer-newsletter-email" placeholder="E-Mail" class="bg-transparent border-b-2 border-paper text-paper placeholder-muted px-0 py-2 focus:outline-none focus:border-primary transition" required>
                    <input type="text" id="footer-newsletter-name" placeholder="Name" class="bg-transparent border-b-2 border-paper text-paper placeholder-muted px-0 py-2 focus:outline-none focus:border-primary transition">
                    <button type="submit" class="border-2 border-paper text-paper hover:border-primary hover:text-primary px-4 py-2 font-mono text-xs uppercase tracking-widest transition mt-2">Abonnieren</button>
                </form>
            </div>
        </div>

        <div class="border-t-2 border-primary mt-12 pt-8">
            <p class="text-center text-muted font-mono text-xs">&copy; <?= date('Y') ?> ZEROWEB. ALLE RECHTE VORBEHALTEN.</p>
        </div>
    </div>

    <script>
        document.getElementById('footer-newsletter-form')?.addEventListener('submit', async (e) => {
            e.preventDefault();
            const email = document.getElementById('footer-newsletter-email').value.trim();
            const name = document.getElementById('footer-newsletter-name').value.trim();
            const submitBtn = e.target.querySelector('button[type="submit"]');
            const originalText = submitBtn.textContent;

            submitBtn.disabled = true;
            submitBtn.textContent = 'Wird abgesendet...';

            try {
                const response = await fetch('/api/newsletter/subscribe', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email, name })
                });
                const data = await response.json();

                if (data.success) {
                    alert('Erfolgreich abonniert!');
                    e.target.reset();
                } else {
                    alert(data.error || 'Fehler beim Abonnieren. Bitte versuche es später erneut.');
                }
                submitBtn.disabled = false;
                submitBtn.textContent = originalText;
            } catch (err) {
                console.error('Newsletter subscription error:', err);
                alert('Verbindungsfehler – bitte versuche es später erneut');
                submitBtn.disabled = false;
                submitBtn.textContent = originalText;
            }
        });
    </script>
</footer>
